# рестор базы с потгисом для с измененным дампом 
cat amur_edited.sql | docker exec -i geogr_api-postgres-1 psql -U postgres
dump-amur22_meta_repl-202401311544_full
# проверить какие контейнеры работают
docker ps
# запустить конкретный контейнер 
docker-compose up -d --no-deps --build web


для каждой variable_id нужно 
1) количество site_id +
2) для каждого сайта нужно минимальная и максимальная дата +
3) значение value минимум, максимум и среднее +
4) нарисовать карту site_id (под вопросом, надо джойнить столбец с координатами)


для произвольной группы site_id по каждой variable_id
1) получить для каждого site_id таблицу date_loc и value 
2) вывести полученный результат в csv для каждого site_id c названием site_id